import { onMount, onCleanup } from 'solid-js'
import { SidebarContainer } from '../../components/SidebarContainer';

export const Home = () => {
  onMount(() => {

  });

  onCleanup(() => {

  });

  return (
    <SidebarContainer>
      Home
    </SidebarContainer>
  );
};
